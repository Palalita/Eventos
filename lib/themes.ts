// Registro de temas visuales que un cliente puede elegir para su evento
// (Organization.theme guarda uno de estos ids). Por ahora hay un solo
// preset real (el que ya tenía el sitio); el selector de temas en
// /crear-cuenta y el resto de los presets son la siguiente iteración — ver
// el plan multi-tenant.
export const DEFAULT_THEME = "xv_rosa_dorado";
